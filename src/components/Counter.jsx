import React from 'react';

const Counter = ({ count }) => {
    return (
        <div className="text-center mt-5">
            <h1>Counter: {count}</h1>
        </div>
    );
};

export default Counter;